/**
 * @module url-parser-cli
 * This is the main library entry point.
 * It exports the core stream processing components.
 */

// --- Library Exports ---
// Export the core components to make this project usable as a library.
export { UrlParser } from './lib/UrlParser.js';
export { UrlProcessor } from './lib/UrlProcessor.js';
export type { OutputData } from './lib/types.js';
